walt-client-selector
====================

This package is "virtual", and only contains a dependency over package walt-client.
It is used to find an appropriate walt-client version for a given
(<server_api>, <client_api>) tuple.
